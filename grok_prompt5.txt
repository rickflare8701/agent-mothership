GROK — Updated session summary for fresh eyes.

## STATE
- Beacon relay (Cloudflare Tunnel + Express:3000) active, connected to library PC
- Tunnel URL: https://firewire-learn-zope-vernon.trycloudflare.com
- PC: ABPCP532, Win10 22H2, PS 5.1, user LC2022 (non-admin)
- AppLocker active, Avecto active (blocks SCM, token creation, schtasks /ru SYSTEM)

## WHAT WORKS
1. STAgentCtl.exe admin bypass via in-memory patching (run_patched_simple.ps1)
2. SYSTEM task dispatch: `dispatch --engine b443f8a1 --operation 9d77c15b --paramData <arg>` runs STAgentUpdater.exe as SYSTEM
3. --paramData is passed RAW as command-line argument

## WHAT WE TRIED AND FAILED

A. **Python ctypes → CDispatchRpcClient**: Constructor succeeds but all dispatch methods throw CDispatchException (custom Ivanti type). The RPC binding handle inside the DLL is fundamentally broken — can't fix from external ctypes.

B. **NdrClientCall3 raw RPC**: STAgentFramework.dll uses MANUAL marshaling (NdrClientInitializeNew/NdrSendReceive/NdrGetBuffer/NdrFreeBuffer/etc.), NOT NdrClientCall2/3. No MIDL format strings exist anywhere in the DLL to extract. This path is dead.

C. **DLL hijack via STAgentUpdater.exe imports**: Checked every DLL in import table. All are either System32 DLLs or from the agent directory (C:\Program Files\LANDESK\Shavlik Protect Agent\). Agent directory is NOT writable. STAgentUpdater.exe has ZERO LoadLibrary calls in .text — no dynamic loading. Delay-load imports: none. No hijack opportunity.

D. **STAgentFramework.dll LoadLibrary**: Also checked — no LoadLibrary calls in .text. Delay-loads: msi.dll, RPCRT4.dll (both System32).

E. **CWD detection**: Tried WMI (Win32_Process), .NET GetProcessById, OpenProcess+NtQueryInformationProcess — all return empty/access-denied for SYSTEM process (STDispatch.exe PID 4180). Cannot determine CWD.

F. **store.dat modification**: Located at C:\ProgramData\LANDESK\Shavlik Protect\Agent\store.dat (0 bytes, writable). Previous research showed replacing with dataCache.dat format caused agent to briefly appear then exit — registration check is in native C++ code, can't be spoofed from file.

G. **dispatch --paramData fuzzing**: Tested -updateData 0, -checkinAndUpdateAll, -reset_counts, -updateBinaries, long strings, URLs as args, file paths — ALL complete in ~15ms. Agent is unregistered, STAgentUpdater.exe exits immediately for ANY command when not registered.

H. **STDispatch directory monitoring**: Confirmed STDispatch does NOT poll any writable directories (no FileSystemWatcher/timer). New\, FTQ\, CustomUpdate\ directories all unwatched.

## KEY DISCOVERIES THIS SESSION

1. **AgentDataDirectory is writable**: `C:\ProgramData\LANDESK\Shavlik Protect\Agent\` is writable by LC2022 and contains:
   - store.dat (0 bytes) — registration state
   - dataCache.dat (816 bytes) — event cache with RegisterAgent event
   - STAgentUpdater\Package\ (empty) — update package dir
   - CustomUpdate\ (empty)
   - New\, Old\, FTQ\ directories
   - Updates\ → junction to C:\Windows\Tasks\

2. **AgentEnvironment.config** at `C:\Program Files\LANDESK\Shavlik Protect Agent\AgentEnvironment.config` shows:
   - agentDataDirectory = C:\ProgramData\LANDESK\Shavlik Protect\Agent\
   - serverUri = //patchlink5.staff.local:3121/ST/Console/AgentState/v2 (unreachable)
   - cloudRegistrationUri = https://isec.ivanticloud.com/privateapi (reachable)

3. **C:\Windows\System32\tasks\ is writable** by LC2022 (confirmed)

4. **dataCache.dat format**: 4-byte size prefix + "Data " magic + UTF-16 JSON event data. Contains a RegisterAgent event with empty agent id.

## REMAINING ATTACK PATHS (need your advice)

### 1. Fake registration via store.dat
The registration check is in native C++ code, but maybe there's a way to provide a valid session/agent ID that survives validation? The agentDataDirectory is writable — could we write ancillary files that the registration check also validates?

### 2. Cloud registration abuse
cloudRegistrationUri = https://isec.ivanticloud.com/privateapi IS reachable. Can we register the agent via the cloud API somehow? Do we need credentials?

### 3. STDispatch.exe internal engine mapping
The engine GUID→binary mapping is inside STDispatch.exe's code. Can we reverse-engineer the mapping function to find a way to register a custom engine? Or is there a second engine we missed?

### 4. Junction/file race
C:\Windows\System32\tasks\ is writable. STDispatch creates tasks/GUID.txt files during dispatch (relative path). If we could predict the GUID or race the file creation, we could influence the task definition. Is the GUID predictable (e.g., sequential, time-based)?

### 5. Python ghost folders for SYSTEM
HKCU PythonPath ghost folders work for user-level Python (LC2022). Can we force a SYSTEM process to import Python somehow? Are there any SYSTEM services on this PC that invoke Python?

### 6. -updateBinaries MITM
STAgentUpdater.exe -updateBinaries presumably downloads new binaries from the console server. The server is unreachable (patchlink5.staff.local). But what if we could redirect the download via hosts file, proxy env var, or DNS? We can write to C:\Windows\System32\drivers\etc\hosts?

### 7. Completely new angle
Is there a completely different approach we haven't considered? Any Windows 10 22H2 privilege escalation technique that works under Avecto + AppLocker?

## QUESTIONS
1. What's the most promising path forward given these constraints?
2. Should we focus on the 1AM reset race (it's currently ~8 hours away)?
3. Is there a way to make the unregistered STAgentUpdater.exe do something useful?
4. Any ideas for detecting the CWD without admin access to the SYSTEM process?
